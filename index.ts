// Q87
// this function extract first 10 character of a string by substring
function extractcharacter(str : string) : string{
    return str.substring(0,10);
}
console.log(extractcharacter("hello! coders "));